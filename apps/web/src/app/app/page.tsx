"use client";

// The unified workbench (models / agents / gallery) is rendered by AppShell.
// This route only needs to mount the shell; content is driven by shell state.
export default function AppPage() {
  return null;
}
